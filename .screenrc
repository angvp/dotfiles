# angvp's screenrc file
# ======================
# I've to admit that I have been using screen since old epoques and today on
# 2011 I decided to make this file :)

startup_message off # removing the crappy start message

# Last line helper
hardstatus alwayslastline "%{yk}( %{Y}%H %{y})%=  %{wk}%?%-Lw%?%{C}(%{W}%n*%f %t%?(%u)%?%{C})%{w}%?%+Lw%?%?%= %{y}( %{Y}load: %l %{y}) (%C %A - %d-%m-%Y)"

# some security shell
idle 3600 lockscreen

# sorry riBer .. this makes me so much fun.
vbell_msg "Nooooo... ESTAMOS EN LA B!!!!"
