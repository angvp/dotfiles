# LazyVim Config (Fixed: lazy-require Telescope)
- Esta versión evita `require('telescope.builtin')` al cargar, así no falla antes de que Lazy instale/active plugins.
Ver README anterior para pasos de instalación.
